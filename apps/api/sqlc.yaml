version: "2"
sql:
  - engine: "sqlite"
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "queries"
        out: "internal/db/queries"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
